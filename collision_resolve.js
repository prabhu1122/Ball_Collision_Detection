class collisionResolve {
    constructor(velocity,angle) {
        vel = velocity;
        ang = angle;

        this.msg = function() {
            // body...
            alert("alert!!")
        }
        
    }
}